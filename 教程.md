# bardecode 使用与原理教程

> 面向零基础读者。你不需要懂 Python，跟着做就能用、能懂。

---

## 目录

1. [这个程序是干什么的](#1-这个程序是干什么的)
2. [一分钟理解它怎么工作](#2-一分钟理解它怎么工作)
3. [准备工作：装好环境](#3-准备工作装好环境)
4. [使用篇：怎样识别条形码](#4-使用篇怎样识别条形码)
5. [原理篇：两张照片的故事](#5-原理篇两张照片的故事)
6. [架构篇：六个抽屉各管一摊](#6-架构篇六个抽屉各管一摊)
7. [代码解读：逐文件读懂它](#7-代码解读逐文件读懂它)
8. [发布篇：把程序给别人用](#8-发布篇把程序给别人用)
9. [常见问题](#9-常见问题)
10. [进阶篇：换模型与自己训练](#10-进阶篇换模型与自己训练)
11. [下一步更新方向](#11-下一步更新方向)

---

## 1. 这个程序是干什么的

给它一张照片（商品包装、快递单、发票、票据都行），它会：

- **找出照片里所有的条形码**（不管是 1D 的竖条纹，还是 2D 的二维码）
- **读出每个码的内容**（那串数字或网址）
- **打印出结构化结果**（JSON 格式，方便别的程序处理）

举个真实的例子。给它下面这张含有 4 个商品条形码的拼图：

```
照片：top-barcodes-in-retail-header.jpg.webp
```

它会输出（精简版）：

```
EAN-13  0065515000037   （检测置信度 0.857）
EAN-13  8435039118747   （检测置信度 0.830）
EAN-13  0037000377085   （检测置信度 0.824）
EAN-13  0065656915641   （检测置信度 0.707）
```

四个码，全找全了。

**特点**：

- ✅ 能识别**多个**条形码（不是只找一个）
- ✅ 在 **CPU 上跑**，不需要显卡（针对弱主机优化）
- ✅ 支持 1D（EAN/UPC/Code128 等）+ 2D（QR/DataMatrix/Aztec/PDF417）
- ✅ 一次安装，**断网也能用**（AI 模型随程序打包）

---

## 2. 一分钟理解它怎么工作

想象你看一张满是条形码的照片，你要把每个码的数字读出来。你会怎么做？

1. **先扫一眼整张图**，找到"哪里有条形码"——用眼睛。
2. **逐个盯住每个码**，把条纹翻译成数字——用你的识码能力。

这个程序完全一样，分两步：

```
┌─────────────────┐     ┌─────────────────┐
│  第一步：找码    │ ──▶ │  第二步：读码    │
│  （AI 模型）     │     │  （解码器）      │
│  "这里有 4 个码" │     │  "这 4 个码是…"  │
└─────────────────┘     └─────────────────┘
   YOLOv8s + ONNX          zxing-cpp
```

- **第一步用 AI**：一个叫 YOLOv8s 的小型神经网络，专门被训练来"认出照片里的条形码框"。它只负责指出位置，不负责读内容。
- **第二步用传统算法**：一个叫 zxing-cpp 的成熟条码解码库（Google 的 ZXing 项目的 C++ 版），负责把条纹翻译成数字。

**为什么要分两步？** 因为各擅胜场：AI 擅长"在乱糟糟的照片里找东西"，解码器擅长"把条纹精确翻译"。合在一起反而不如分工。这叫**让专业的人做专业的事**。

---

## 3. 准备工作：装好环境

你需要两样东西：

### 3.1 装 Python（如果还没有）

到 [python.org](https://www.python.org/downloads/) 下载 Python **3.10 或更新版本**。安装时**勾选 "Add Python to PATH"**（重要）。

打开 PowerShell（Windows）或终端（Mac/Linux），输入：

```powershell
python --version
```

看到 `Python 3.10.x` 或更高就行。

### 3.2 装 uv（一个 Python 包管理小工具）

`uv` 是个几百 KB 的小程序，专门用来管理 Python 项目，比传统的 pip 快很多。

```powershell
# Windows PowerShell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

装完，**关闭并重新打开 PowerShell**，然后验证：

```powershell
uv --version
```

看到版本号就成功了。

### 3.3 拿到这个项目

如果项目已经在你的电脑上（比如在 `D:\Temp\opencv-barcode`），跳过。否则把它下载/克隆下来。

### 3.4 安装项目依赖（一次性）

在项目根目录（有 `pyproject.toml` 的那个目录）执行：

```powershell
uv sync --extra dev
```

这会自动创建一个独立的 Python 环境，把所有需要的库（ONNX 运行时、OpenCV、zxing-cpp、Pillow 等）一次性装好。**断网后这些就不需要再装了。**

看到 `Resolved X packages` 之类的提示就完成了。

---

## 4. 使用篇：怎样识别条形码

### 4.1 最简单的用法

```powershell
uv run python -m bardecode 你的照片.jpg
```

例如：

```powershell
uv run python -m bardecode tests\fixtures\top-barcodes-in-retail-header.jpg.webp
```

会打印一段 JSON：

```json
{
  "tool": "bardecode",
  "version": "0.1.0",
  "results": [
    {
      "image": "tests\\fixtures\\top-barcodes-in-retail-header.jpg.webp",
      "width": 1526,
      "height": 720,
      "barcodes": [
        {
          "format": "EAN-13",
          "text": "0065515000037",
          "bbox": [2, 54, 350, 548],
          "detection_score": 0.857
        },
        ...
      ],
      "undecoded": [...],
      "error": null
    }
  ]
}
```

**字段含义**：

| 字段 | 含义 |
|---|---|
| `format` | 码的类型，如 `EAN-13`、`QRCode`、`DataMatrix` |
| `text` | 解码出的内容（数字串、网址等） |
| `bbox` | 码在照片里的方框位置 `[左, 上, 右, 下]`（像素坐标） |
| `detection_score` | AI 找到这个码的把握有多大，0~1，越大越确定 |
| `undecoded` | AI 觉得"这里有码"但解码器读不出的区域 |
| `error` | 这张图有没有出错（`null` = 正常） |

### 4.2 只想要码值（最干净的输出）

如果你只关心码的内容，不想要那些坐标、置信度：

```powershell
uv run python -m bardecode -q 你的照片.jpg
```

输出（一行一个码）：

```
0065515000037
8435039118747
0037000377085
0065656915641
```

可以很方便地存到文件：

```powershell
uv run python -m bardecode -q *.jpg > 所有码.txt
```

### 4.3 一次处理多张图

```powershell
uv run python -m bardecode img1.jpg img2.png img3.webp
```

或者用通配符（PowerShell 里用引号包起来）：

```powershell
uv run python -m bardecode "*.jpg"
```

每张图的结果都在 `results` 数组里分开。

### 4.4 只找特定类型的码

比如只要二维码（QR Code），不要一维条码：

```powershell
uv run python -m bardecode --formats QRCode 票据.png
```

多个类型用逗号分隔：

```powershell
uv run python -m bardecode --formats EAN13,QRCode 包装.jpg
```

**支持的类型名**（大小写敏感，要完全匹配）：`EAN13`、`EAN8`、`UPCA`、`UPCE`、`QRCode`、`DataMatrix`、`Aztec`、`PDF417`、`Code39`、`Code93`、`Code128`、`Codabar`、`ITF` 等。

### 4.5 找不到码？调低门槛

默认情况下，AI 只有 25% 把握（`0.25`）以上的"码"才会报出来。如果你的照片比较糊、或者码比较小，可以调低这个门槛：

```powershell
uv run python -m bardecode --conf-threshold 0.1 模糊的照片.jpg
```

反过来，如果你发现误报太多（把不是码的东西当成码），调高：

```powershell
uv run python -m bardecode --conf-threshold 0.5 清晰照片.jpg
```

### 4.6 看诊断信息

加 `-v` 会在错误流（stderr）打印每张图的处理概况：

```powershell
uv run python -m bardecode -v 照片.jpg
```

会看到类似：

```
[bardecode] 照片.jpg: 4 decoded, 2 undecoded, error=None
```

### 4.7 完整参数一览

```
bardecode [选项] 图片 [图片 ...]

--conf-threshold FLOAT   检测置信度阈值（默认 0.25）
--iou-threshold FLOAT    重叠框合并阈值（默认 0.45）
--img-size INT           推理尺寸（默认 640，一般不用改）
--model PATH             用自己的 ONNX 模型替换内置的
--formats STR            码类型白名单（逗号分隔）
--no-fallback            禁用"全图兜底解码"
-q, --quiet              只打印码值
-v, --verbose            打印诊断到 stderr
--version                显示版本号
```

---

## 5. 原理篇：两张照片的故事

### 5.1 第一步：AI 怎么"找"码

AI 模型叫 **YOLOv8s-barcode**。我们来拆解这个名字：

- **YOLO** = *You Only Look Once*（只看一眼）——一种快速目标检测算法
- **v8** = 第 8 代（很新）
- **s** = small（小型版，约 22MB 参数量）
- **-barcode** = 被人在几万张条形码图片上"特训"过，专门识别条形码

它的工作方式可以这样理解：

1. **输入**：一张 640×640 像素的图（如果原图不是正方形，会被等比缩放后用灰边补齐——这叫 **letterbox**）。
2. **脑内运算**：神经网络把图划成 8400 个候选位置，每个位置预测"这里有码的把握有多大"和"码的大致方框"。
3. **输出**：8400 个候选，每个带 4 个坐标（中心点 + 宽高）和一组置信度。

但 8400 个里大多数是垃圾——要么把握极低，要么互相重叠。所以要做两道清理：

- **置信度过滤**：扔掉把握低于阈值的（默认 0.25）。
- **NMS（非极大值抑制）**：同一位置上往往有多个高度重叠的候选框，NMS 只保留把握最高的那一个，抑制其余。

> 💡 **类比**：NMS 就像评选比赛——同一个选手（同一个码）报了 5 次名，只保留分数最高的那次报名，其他 4 次作废。这样最终每个码只被框一次。

清理后，剩下的就是"照片里每个码的方框位置"。

### 5.2 第二步：解码器怎么"读"码

对每个方框，程序做：

1. **从原图裁出方框区域**（一个只有那个码的小图）。
2. **交给 zxing-cpp**：这是 Google ZXing 项目的 C++ 高性能版本，能识别几乎所有常见条码格式。
3. **拿回解码结果**：码的类型（如 `EAN-13`）和文本内容（如 `0065515000037`）。

zxing-cpp 用的是**经典计算机视觉算法**（扫描条纹宽度、查表、纠错），不是 AI。它成熟、精确、快速——所以"读码"这一步不需要 AI。

### 5.3 一道兜底：如果裁剪解码全失败

有时候 AI 找到了码，但裁出来的小图由于边缘对不齐，解码器读不出来。这时程序会**回退用整张原图再调一次 zxing-cpp**——zxing 自带的找码能力会兜底漏掉的情况。

如果整图也读不出，那个位置会被列入 `undecoded`（"检测到但读不出"），便于你后续人工查看。

### 5.4 为什么这样设计很省资源

- **AI 模型只有 22MB**（参数量），CPU 上推理一张图大约几百毫秒。
- **模型被打包进程序里**（不是临时下载），所以弱主机装一次就能离线用。
- **运行时完全不需要 PyTorch**（PyTorch 是训练 AI 用的"重型工具"，大约 800MB）。我们用的是 **ONNX Runtime**——一个轻量的 AI 推理引擎（约 50MB），专门用来"运行"训练好的模型。

> 💡 **类比**：PyTorch 像 Photoshop（用来制图的大软件），ONNX Runtime 像图片查看器（只负责显示，轻得多）。我们只需要"显示"，所以用轻的那个。

### 5.5 什么是 ONNX

**ONNX**（Open Neural Network Exchange）是 AI 模型的**通用交换格式**。就像 PDF 不管你用什么软件做的都能打开一样，ONNX 模型不管你用什么框架训练的（PyTorch、TensorFlow 都行），都能用 ONNX Runtime 加载运行。

我们的开发流程：

```
开发者电脑（一次性）                用户电脑（每次运行）
──────────────────                ──────────────────
PyTorch + .pt 权重                
       │                          
       ▼ 导出                     
   ONNX 文件 ───────────────────▶ ONNX Runtime 加载推理
   （22MB，打包进程序）            （轻量，无需 PyTorch）
```

---

## 6. 架构篇：六个抽屉各管一摊

整个程序分成 6 个源文件 + 1 个脚本，每个文件像抽屉一样只放一类东西：

```
bardecode/
├── __init__.py        ← 包的"身份证"，只写版本号
├── __main__.py        ← 命令行入口：解析参数、调度流程
├── pipeline.py        ← 总指挥：读图→检测→裁剪→解码→汇总
├── detector.py        ← AI 检测器：加载 ONNX、推理、后处理
├── decoder.py         ← 解码器：调用 zxing-cpp
├── image_io.py        ← 图像读写：读图（含 webp 兜底）+ 输出 JSON
├── schemas.py         ← 数据模板：定义各种"填表格式"
└── models/
    └── yolov8s_barcode.onnx   ← AI 模型（22MB，打包进 wheel）

scripts/
└── export_onnx.py     ← 一次性脚本：训练用的 .pt → 运行用的 .onnx

tests/                  ← 自动化测试（44 个，保证质量）
```

**数据像流水一样穿过这些抽屉**：

```
用户在命令行输入图片路径
         │
         ▼
   __main__.py     解析参数，构造检测器
         │
         ▼
   pipeline.py     对每张图：
         │         ├─ image_io.read_image()  读图
         │         ├─ detector.detect()       AI 找码
         │         ├─ decoder.decode_region() 解码每个码
         │         └─ 汇总成 ImageResult
         │
         ▼
   image_io.format_report_json()    转成 JSON
         │
         ▼
   打印到屏幕
```

每个抽屉（文件）只跟它紧邻的抽屉打交道，**不越权**。这样的好处：

- 改一个抽屉不会弄坏别的（比如换 AI 模型只动 `detector.py`）。
- 测试时可以单独测一个抽屉，不用启动整个程序。

---

## 7. 代码解读：逐文件读懂它

> 即使你不会 Python，下面这些解释也能让你看懂"每段代码在做什么"。

### 7.1 `schemas.py` —— 数据模板（最简单，先看这个）

这个文件定义了程序里流通的几种"表格格式"。Python 里用 `@dataclass`（数据类）来声明，可以理解成"填表模板"：

```python
@dataclass
class Detection:          # 一个检测到的方框
    x1: int               # 左
    y1: int               # 上
    x2: int               # 右
    y2: int               # 下
    score: float          # AI 的把握（0~1）

@dataclass
class DecodedBarcode:     # 一个已解码的条码
    format: str           # 类型，如 "EAN-13"
    text: str             # 内容，如 "0065515000037"
    bbox: list[int]       # 在图里的方框位置
    detection_score: float

@dataclass
class ImageResult:        # 处理一张图的结果
    image: str            # 图片路径
    width: int
    height: int
    barcodes: list        # 解码成功的码
    undecoded: list       # 检测到但读不出的位置
    error: str | None     # 出错信息（没出错就是 None）

@dataclass
class Report:             # 整次运行的总报告
    tool: str
    version: str
    results: list         # 每张图一个 ImageResult
```

**为什么先定义这些？** 因为后面所有文件都按这些"表格"来传递数据，就像先约定好表格格式，大家才能填表交流。

### 7.2 `image_io.py` —— 读图与输出

**读图函数** `read_image(path)`：

```python
def read_image(path: str):
    img = cv2.imread(path, cv2.IMREAD_COLOR)    # 先用 OpenCV 读
    if img is None:                              # 读不了？
        img = _read_via_pillow(path)             # 退回用 Pillow 读
        if img is None:                          # 还是读不了？
            raise FileNotFoundError(...)          # 报错"文件不存在"
    return img
```

**为什么要两个读法？** OpenCV 速度快，但某些格式（比如有的 webp 编译版本）它读不了；Pillow（PIL）是个备用读图库，格式支持更广。这个"双保险"保证用户给的图基本都能读。

**输出函数** `format_report_json(report, quiet)`：

- `quiet=True` 时，只把所有码的内容拼成一行一个的文本。
- 默认输出格式化好的 JSON。

### 7.3 `decoder.py` —— 解码器封装

```python
def decode_region(image, formats=None):
    results = zxingcpp.read_barcodes(image, ...)  # 调 zxing-cpp
    out = []
    for r in results:
        # 把 zxing 返回的位置（4 个角点）整理成统一格式
        pos = [(r.position.top_left.x, r.position.top_left.y), ...]
        out.append(Decoded(format=..., text=..., position=pos))
    return out
```

这个文件就是个**薄包装**——把 zxing-cpp 的接口稍微整理一下，方便程序其他部分用。

注意 `_resolve_formats` 函数：它把用户传的字符串（如 `"EAN13"`）转换成 zxing-cpp 认识的内部枚举值。如果传错了名字（比如 `"EAN_13"`），它会列出所有合法名字帮你纠正。

### 7.4 `detector.py` —— AI 检测器（最复杂，慢慢看）

这个文件有 4 个部分：

**(a) `letterbox` 函数** —— 把任意尺寸的图塞进 640×640 的正方形：

```
原图（比如 1526×720）                 letterbox 后（640×640）
┌────────────────────┐               ┌──────────────────┐
│                    │               │ 灰灰灰灰灰灰灰灰 │
│                    │   等比缩放    │ ┌────────────────┐│
│      照片内容       │  ─────────▶  │ │                ││
│                    │   多余部分    │ │   缩放后的内容   ││
│                    │   用灰填充    │ │                ││
└────────────────────┘               │ └────────────────┘│
                                     │ 灰灰灰灰灰灰灰灰 │
                                     └──────────────────┘
```

为什么要这样？因为 AI 模型只认 640×640 的输入。我们要保持原图比例（不拉伸变形），不够的地方用灰色（RGB 114,114,114）补齐。

**返回三个东西**：处理后的图、缩放比例、灰边宽度——后两个用来在最后把"640 空间的坐标"还原回"原图的坐标"。

**(b) `postprocess` 函数** —— 把 AI 的原始输出整理成"方框列表"：

```
AI 原始输出（[1, 6, 8400]：8400 个候选）
        │
        ▼ 置信度过滤（扔掉把握 < 0.25 的）
        │
        ▼ NMS 合并重叠框
        │
        ▼
  最终的 Detection 列表（每个码一个方框）
```

**关键细节**：调用 `cv2.dnn.NMSBoxes` 时，必须传"左上角 + 宽高"格式，不能传"中心点 + 宽高"——否则合并结果会错。代码里有专门注释和回归测试守住这个细节。

**(c) `_preprocess` 函数** —— 把图变成 AI 能吃的格式：

```python
rgb = boxed[:, :, ::-1]              # BGR → RGB（OpenCV 默认 BGR，AI 要 RGB）
norm = rgb.astype(np.float32) / 255.0  # 像素值从 0~255 归一化到 0~1
nchw = norm.transpose(2, 0, 1)[None, ...]  # 排列成 (1, 3, H, W)
```

**(d) `BarcodeDetector` 类** —— 把上面三步串起来：

```python
class BarcodeDetector:
    def __init__(self, ...):
        # 加载 ONNX 模型（只加载一次）
        self.session = ort.InferenceSession(path, providers=["CPUExecutionProvider"])

    def detect(self, img):
        boxed, ratio, (pad_w, pad_h) = letterbox(img, self.img_size)  # 1. 塞框
        blob = self._preprocess(boxed)                                 # 2. 整理
        raw = self.session.run(None, {self.input_name: blob})[0]       # 3. AI 推理
        dets_640 = postprocess(raw, ...)                               # 4. 后处理
        # 5. 把 640 空间的坐标，还原回原图坐标
        for d in dets_640:
            x1 = int((d.x1 - pad_w) * ratio)
            ...
```

**关键**：`BarcodeDetector` 在程序启动时**只加载一次模型**，之后每张图都复用——避免反复读 22MB 的 ONNX 文件。

### 7.5 `pipeline.py` —— 总指挥

这是核心流程：

```python
def process_image(path, ...):
    # 1. 读图
    img = read_image(path)
    
    # 2. AI 找码
    detections = detector.detect(img)
    
    # 3. 对每个检测到的方框，裁出来解码
    for d in detections:
        crop = img[d.y1:d.y2, d.x1:d.x2]      # 裁剪
        results = decode_region(crop)          # 解码
        if results:
            barcodes.append(...)               # 成功 → 加入 barcodes
        else:
            undecoded.append(...)              # 失败 → 加入 undecoded
    
    # 4. 如果全失败，回退用整图解码
    if not barcodes:
        full_results = decode_region(img)
        ...
    
    # 5. 去重（同一个码可能被识别多次）
    barcodes = 去重(barcodes)
    
    return ImageResult(...)
```

**亮点**：每一步出错（图读不了、AI 崩了、解码异常）都不会让整个程序挂掉，而是把错误记到 `ImageResult.error` 里，继续处理下一张图。

### 7.6 `__main__.py` —— 命令行入口

```python
def main():
    # 1. 解析命令行参数（图片路径、各种选项）
    args = parse_args()
    
    # 2. 构造检测器（用用户给的参数）
    detector = BarcodeDetector(conf_thres=args.conf_threshold, ...)
    
    # 3. 逐张处理
    for path in args.images:
        ir = process_image(path, detector=detector, ...)
        results.append(ir)
    
    # 4. 输出 JSON
    print(format_report_json(report, quiet=args.quiet))
    
    # 5. 返回退出码（0=成功，1=有错，2=文件没找到）
    return exit_code
```

退出码的含义：

| 退出码 | 含义 | 怎么观察 |
|---|---|---|
| 0 | 全部成功 | PowerShell 不显示任何错误 |
| 1 | 有一般性错误 | 部分图处理失败 |
| 2 | 有文件找不到 | 至少一张图路径错了 |

可以这么用：

```powershell
uv run python -m bardecode 照片.jpg
echo $LASTEXITCODE    # 看上一条命令的退出码
```

### 7.7 `scripts/export_onnx.py` —— 一次性脚本（与日常使用无关）

这个脚本**只在开发期用一次**：把开发者训练好的 `.pt`（PyTorch 格式）模型转换成 `.onnx`（运行格式），然后删掉中间的 `.pt`。普通用户**不需要运行它**——你装的程序里已经带 `.onnx` 了。

---

## 8. 发布篇：把程序给别人用

### 8.1 程序的两种存在形态

```
形态 A：源码项目              形态 B：wheel 安装包
（你电脑上现在的样子）        （要给别人用的样子）
├── bardecode/                一个 .whl 文件
├── tests/                    （37.7MB，含 AI 模型）
├── pyproject.toml            
└── ...一大堆文件             
```

**给别人用，给一个 wheel 文件就够了。**

### 8.2 构建 wheel

在项目根目录：

```powershell
uv build
```

完成后，`dist/` 目录里会出现：

```
dist/
├── bardecode-0.1.0-py3-none-any.whl     ← 这就是安装包（37.7MB）
└── bardecode-0.1.0.tar.gz               ← 源码包（一般用不到）
```

> 💡 **`.whl` 是什么？** 它是 Python 的标准安装包格式，本质是个 zip 压缩包。你可以理解为"Python 版的 .exe 安装程序"。里面已经打包了 AI 模型，别人装的时候不需要联网下载模型。

### 8.3 给别人用（三种场景）

**场景 1：对方电脑上没装 Python**

让对方先装 Python 3.10+（见第 3.1 节），然后：

```powershell
# 对方在自己电脑上执行
pip install bardecode-0.1.0-py3-none-any.whl
```

`pip` 会自动从网上拉几个轻量依赖（onnxruntime、opencv、zxing-cpp），然后装好 bardecode。装完直接用：

```powershell
bardecode 照片.jpg
```

注意此时用的是 `bardecode` 命令（不是 `python -m bardecode`），因为 `pip install` 会自动注册一个命令行入口。

**场景 2：对方电脑完全是离线的（不能联网）**

稍麻烦——需要先在有网的机器上把所有依赖下载下来，一起拷过去。具体步骤：

```powershell
# 有网的机器上，准备一个文件夹装所有包
mkdir bundle
cd bundle

# 下载 wheel 本体
copy ..\dist\bardecode-0.1.0-py3-none-any.whl

# 下载所有依赖（一次性）
pip download bardecode-0.1.0-py3-none-any.whl -d .

# 把整个 bundle 文件夹拷到离线机器，然后：
pip install --no-index --find-links=. bardecode-0.1.0-py3-none-any.whl
```

**场景 3：给同事/朋友在自己机器上试用**

最简单——把整个项目文件夹拷给他，他执行：

```powershell
uv sync --extra dev
uv run python -m bardecode 照片.jpg
```

### 8.4 升级到新版本

改完代码后，更新版本号。打开 `pyproject.toml`：

```toml
version = "0.1.0"    # 改成 "0.2.0"
```

还有 `bardecode/__init__.py`：

```python
__version__ = "0.1.0"    # 同步改成 "0.2.0"
```

然后重新 `uv build`，`dist/` 里会出现新版号的 wheel。

### 8.5 发布到 PyPI（让全世界都能 `pip install`）

如果你想让任何人都能 `pip install bardecode` 安装，可以发布到 [PyPI](https://pypi.org)（Python 官方包仓库）。简单流程：

1. 注册 PyPI 账号
2. 安装上传工具：`uv tool install twine`
3. 上传：`twine upload dist\bardecode-0.1.0-py3-none-any.whl`

> ⚠️ 但 37.7MB 的包（含 AI 模型）发布到 PyPI 前，建议先考虑：是否真的需要全世界的用户？如果只是内部用，私有分发 wheel 文件就够了。

---

## 9. 常见问题

### Q1：报错"Cannot read image"

图片路径写错了，或者文件不是图片。检查：
- 路径里的反斜杠 `\` 和引号（PowerShell 路径含空格要加引号）
- 文件是否真的存在：`Test-Path 你的图片.jpg`
- 文件后缀是否是支持的格式（jpg/png/webp/bmp/tiff）

### Q2：AI 检测到了码，但解码失败（出现在 undecoded 里）

可能原因：
- 码被部分遮挡、模糊、反光
- 码太小（裁出来后分辨率不够）

尝试：
- 调低置信度看更多候选：`--conf-threshold 0.1`
- 用更高清的原图

### Q3：处理速度慢

第一张图会慢一些（要加载 AI 模型，约 1~2 秒），之后每张图 0.5~2 秒（取决于图片大小和 CPU）。这是 CPU 推理的正常速度。

如果实在要更快，可以试 `--img-size 320`（牺牲一点精度换速度）。

### Q4：报错找不到 ONNX 模型

如果你用 `--model` 指定了自定义模型路径，检查路径是否正确。如果用默认内置模型，确认 wheel 装好了。

### Q5：支持 GIF / 视频吗

不支持。只支持**静态图片**（jpg/png/webp/bmp/tiff）。视频需要逐帧截取后再处理。

### Q6：能批量处理一个文件夹吗

可以，用通配符：

```powershell
uv run python -m bardecode "C:\照片\*.jpg"
```

PowerShell 下通配符要加引号。

### Q7：结果是乱码 / 中文

默认 JSON 输出已经是 UTF-8 编码。如果 PowerShell 显示乱码，先执行：

```powershell
chcp 65001
```

---

## 10. 进阶篇：换模型与自己训练

程序内置的 AI 模型不是唯一的。你可以换一个更好的，甚至自己训练一个。本章教你怎么做。

### 10.1 这个程序用的模型是从哪找来的

内置模型叫 **`Piero2411/YOLOV8s-Barcode-Detection`**，托管在 [HuggingFace](https://huggingface.co/Piero2411/YOLOV8s-Barcode-Detection)。当时的查找过程：

1. 调 HuggingFace 搜索 API，关键词 `barcode`
2. 按**下载量排序**——下载量高通常意味着质量经过更多人验证
3. 候选里有几个：
   - `Piero2411/YOLOV8s-Barcode-Detection`（1529 下载，最多）✅ 选了这个
   - `2z299/rtdetr-v2-r50-barcode-finetune-onnx`（已 ONNX，但 r50 模型太大）
   - 其他下载量都只有个位数
4. 进入仓库看文件列表，确认有 `.pt` 权重 + 训练曲线图（说明作者认真训练过）

**选型原则**：下载量 > 训练文档完整度 > 模型大小。下载量是最强的质量信号。

### 10.2 一般去哪里找模型

按优先级（计算机视觉领域）：

| 平台 | 网址 | 特点 | 适合找什么 |
|---|---|---|---|
| **HuggingFace** | huggingface.co | 最大模型库，有 API 可程序化搜索 | 通用模型、NLP、CV |
| **Roboflow Universe** | universe.roboflow.com | CV 专用，**数据集 + 预训练模型都有** | 检测/分割模型、训练数据 |
| **GitHub** | github.com | 搜 `yolov8 barcode`，看 README | 找项目代码 + 配套权重 |
| **Ultralytics Hub** | hub.ultralytics.com | YOLO 官方平台 | YOLO 系列模型 |
| **ONNX Model Zoo** | github.com/onnx/models | 官方经典模型（ResNet 等） | 通用基础模型 |

**搜索关键词组合**（以 barcode 为例）：
- `barcode detection`（最泛）
- `barcode yolov8`（指定架构）
- `barcode onnx`（指定格式，找到就能直接用）
- `barcode pytorch`（指定框架）

**评估一个模型的 5 个信号**：
1. ⭐ 下载量 / 点赞数（社区验证）
2. README 是否有训练细节（数据集、epoch、mAP 精度指标）
3. 是否有评测图（loss 曲线、混淆矩阵）
4. License 是否允许你用（MIT/Apache 最自由）
5. 最近更新时间（太久没更新可能跟不上新版本）

**HuggingFace API 搜索示例**（可以直接在 PowerShell 跑）：

```powershell
# 搜 barcode 相关模型，按下载量排序，取前 10
(Invoke-RestMethod "https://huggingface.co/api/models?search=barcode&full=true") `
    | Sort-Object downloads -Descending `
    | Select-Object id, downloads -First 10
```

### 10.3 怎么换一个别的模型

如果你找到了一个更好的 ONNX 模型，替换很简单：

```powershell
# 1. 备份原模型
move bardecode\models\yolov8s_barcode.onnx bardecode\models\yolov8s_barcode.onnx.bak

# 2. 把新模型复制过来，文件名保持 yolov8s_barcode.onnx
copy 你的新模型.onnx bardecode\models\yolov8s_barcode.onnx

# 3. 测试
uv run python -m bardecode 测试图.jpg
```

> ⚠️ 注意：新模型必须是 **YOLOv8 格式** 的 ONNX（输入 640×640，输出符合 YOLOv8 的张量布局）。如果是别的架构（如 RT-DETR、SSD），需要改 `detector.py` 的后处理代码。

也可以用 `--model` 参数临时指定，不替换原文件：

```powershell
uv run python -m bardecode --model 你的新模型.onnx 照片.jpg
```

### 10.4 怎么自己训练一个专属模型

**最省心的路径：用 Ultralytics（YOLOv8 官方工具）+ 现成数据集。**

整体流程：

```
1. 找/标注数据集  →  2. 写配置文件  →  3. 训练  →  4. 验证  →  5. 导出 ONNX
```

#### 第 1 步：准备数据集（最关键）

**最快的办法——直接用现成数据集**：到 [Roboflow Universe](https://universe.roboflow.com) 搜 `barcode`，能找到几十个已经标注好的数据集，一键下载 **YOLOv8 格式**。

YOLOv8 格式长这样：

```
dataset/
├── images/
│   ├── train/   ← 训练图片（.jpg）
│   └── val/     ← 验证图片
├── labels/
│   ├── train/   ← 标注（.txt，每行一个框）
│   └── val/
└── data.yaml    ← 配置文件
```

每个 `.txt` 标注文件每行格式：`类别编号 中心x 中心y 宽 高`（坐标都归一化到 0~1）：

```
0 0.45 0.52 0.20 0.08
0 0.78 0.31 0.15 0.06
```

**自己标注**（如果没有现成数据集）：
- 工具：[Label Studio](https://labelstud.io)（免费、好用）或 [CVAT](https://cvat.org)
- 至少标注 **几百到几千张**图片，覆盖各种角度、光照、背景
- 类别设计：可以只设 1 类（`barcode`），也可以分 2 类（`barcode_1d` / `barcode_2d`）

#### 第 2 步：写配置文件 `data.yaml`

```yaml
path: ./dataset          # 数据集根目录
train: images/train      # 训练图片相对路径
val: images/val          # 验证图片相对路径

names:
  0: barcode             # 类别 0 叫 barcode
  # 如果分两类：
  # 0: barcode_1d
  # 1: barcode_2d
```

#### 第 3 步：训练（一行命令）

装 ultralytics 并启动训练（建议用 `uv run --with`，不污染项目环境）：

```powershell
uv run --with ultralytics yolo detect train `
    data=data.yaml `
    model=yolov8s.pt `
    epochs=100 `
    imgsz=640 `
    batch=16
```

参数解释：
- `model=yolov8s.pt`：基于 YOLOv8s **预训练权重**做迁移学习（不是从零开始，所以几百张图就够）
- `epochs=100`：把数据集完整看 100 遍
- `imgsz=640`：训练图像尺寸
- `batch=16`：每批 16 张（内存不够就调小到 8 或 4）

**模型大小选择**：

| 变体 | 参数量 | 速度 | 精度 | 适合 |
|---|---|---|---|---|
| `yolov8n.pt` | 3MB | 最快 | 较低 | 弱主机、实时 |
| `yolov8s.pt` | 11MB | 快 | 中等 | **一般推荐** |
| `yolov8m.pt` | 26MB | 中 | 较高 | 追求精度 |
| `yolov8l.pt` | 44MB | 慢 | 高 | 有 GPU |

弱 CPU 主机推荐 `n` 或 `s`。

#### 第 4 步：训练产物

训练完，权重在 `runs/detect/train/weights/best.pt`（最优）和 `last.pt`（最后一轮）。先验证效果：

```powershell
uv run --with ultralytics yolo detect predict model=runs/detect/train/weights/best.pt source=测试图.jpg
```

会输出画了检测框的图，肉眼检查找得准不准。

#### 第 5 步：导出 ONNX（给 bardecode 用）

```powershell
uv run --with ultralytics yolo export `
    model=runs/detect/train/weights/best.pt `
    format=onnx imgsz=640 opset=12 simplify=True
```

生成 `best.onnx`。然后按第 10.3 节的方法替换进项目就能用了。

### 10.5 训练的实际成本

- **数据**：Roboflow 现成 barcode 数据集约 1000~10000 张，免费可下
- **算力**：CPU 上训 100 epoch 约几小时~半天；有 GPU 的话几十分钟
- **代码**：一行 `yolo train` 命令，不需要写训练脚本
- **经验门槛**：迁移学习很傻瓜，难点在**数据质量**（标注准不准、场景多不多）

### 10.6 给你的建议

如果你想自己训一个**更小更快**的模型（比如 YOLOv8**n**，3MB，比现在的 s 版快一倍）：

1. 去 Roboflow Universe 搜 `barcode`，挑下载量高的数据集，导出格式选 **YOLOv8**
2. 用上面的训练命令，把 `model=yolov8s.pt` 改成 `model=yolov8n.pt`
3. 训练完导出 ONNX 替换进项目

如果训出来精度（mAP）和 Piero2411 的 s 版差不多，那你就有一个更轻量的专属模型了。

---

## 11. 下一步更新方向

这是 v0.1.0。以下是后续可以考虑的方向（按优先级）：

### 10.1 短期（易做，价值高）

- **支持视频/摄像头流**：逐帧识别，做成"实时扫码"工具。
- **可视化输出**：加一个 `--draw` 选项，把检测到的方框画在图上另存为新图，方便人工核对。
- **直接输出到文件**：加 `--output result.json` 选项，省去 shell 重定向（Windows 重定向有时编码会乱）。
- **`--formats` 友好别名**：现在要写 `EAN13`（精确大小写），可以支持 `ean13`、`EAN-13`、`qr` 等常见写法。

### 10.2 中期（需要一定工作）

- **更轻量的模型**：目前是 YOLOv8**s**（22MB）。如果有更小的 YOLOv8**n** barcode 权重开源，可以替换成 ~3MB 的，运行更快。
- **模型按需下载**：把 AI 模型从 wheel 里拿出来，首次运行时从网络下载到用户缓存目录。这样 wheel 本体只有几百 KB，分发更轻便。
- **GPU 加速**：目前强制 CPU。如果目标机器有显卡，可以加 `--device gpu` 选项，用 onnxruntime 的 CUDA provider。
- **多进程批量**：处理几百张图时，用多进程并行（目前是串行）。

### 10.3 长期（大改造）

- **训练自己的模型**：用自己手头的条形码图片微调一个专属模型，精度会更高。
- **倾斜/透视矫正**：目前裁剪是矩形框。如果码严重倾斜，可以加透视变换把码"摆正"再解码，提高识别率。
- **Web 界面**：包一个 Flask/FastAPI 网页，用户拖拽上传图片，浏览器里看结果——给不会命令行的人用。
- **多码类型混合识别优化**：AI 模型其实输出了 2 个类（可能是 1D/2D 区分），目前没利用这个信息。可以利用类别来为解码器预设更准确的参数。

### 10.4 测试与质量

- 补 fallback 路径、退出码 1 路径的测试覆盖
- 加一个真实多码场景的回归测试集（防止模型升级后退化）
- 加性能基准测试（每张图耗时统计）

---

## 附：快速命令速查

```powershell
# 首次准备
uv sync --extra dev

# 处理一张图（JSON 输出）
uv run python -m bardecode 照片.jpg

# 只要码值
uv run python -m bardecode -q 照片.jpg

# 多张图
uv run python -m bardecode a.jpg b.png c.webp

# 只要二维码
uv run python -m bardecode --formats QRCode 票.png

# 找不到码时调低门槛
uv run python -m bardecode --conf-threshold 0.1 糊照片.jpg

# 看诊断
uv run python -m bardecode -v 照片.jpg

# 构建安装包
uv build
# 产物在 dist\bardecode-0.1.0-py3-none-any.whl

# 给别人装
pip install bardecode-0.1.0-py3-none-any.whl
# 装完直接用：bardecode 照片.jpg

# 跑测试
uv run pytest                  # 全部
uv run pytest -m "not slow"    # 跳过加载模型的慢测试
```

---

*本教程适用于 bardecode v0.1.0。如有疑问，对照源码和 README.md。*
